[project]
name = "crm-service"
version = "0.1.0"
description = "Lead management microservice for Aerolytic CRM"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]